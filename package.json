{
  "private": true,
  "name": "umzug-example",
  "scripts": {
    "db:migrate": "ts-node -r dotenv-defaults/config cli/migrate",
    "db:seed": "ts-node -r dotenv-defaults/config cli/seed"
  },
  "dependencies": {
    "dotenv-defaults": "^2.0.1",
    "pg": "^8.6.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.6.2",
    "umzug": "^3.0.0-beta.16"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "@types/validator": "^13.1.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.2.3"
  }
}
